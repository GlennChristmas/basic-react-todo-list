import React from "react";

const EmptyFieldsWarning = () => {
  return (
    <div className="empty-fields-warning">
      You have empty fields in your task. You need to complete these before
      clicking add.
    </div>
  );
};

export default EmptyFieldsWarning;
